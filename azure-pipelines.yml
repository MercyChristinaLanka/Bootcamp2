trigger:
- main

stages:
- stage: Debug
  jobs:
  - job: DebugJob
    pool:
      vmImage: ubuntu-latest

    steps:
    - checkout: self

    - script: |
        echo "ROOT PATH:"
        pwd

        echo "FULL REPO STRUCTURE:"
        ls -R
      displayName: "DEBUG REPO STRUCTURE"