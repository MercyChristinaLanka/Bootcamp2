steps:
- checkout: self

- task: TerraformInstaller@1
  displayName: Install Terraform
  inputs:
    terraformVersion: '1.5.0'

- script: |
    echo "Checking Terraform..."
    terraform version
    which terraform
  displayName: Verify Terraform Install